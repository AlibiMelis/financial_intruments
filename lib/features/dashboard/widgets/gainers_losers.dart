import 'package:flutter/material.dart';

class GainersLosersWidget extends StatelessWidget {
  const GainersLosersWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Gainers & Losers');
  }
}
